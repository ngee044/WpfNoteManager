<UserControl x:Class="WpfNoteManager.Views.NoteEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="Auto" Width="Auto">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Title:" FontWeight="Bold"/>
        <TextBox Grid.Row="0" Margin="50,0,0,0"
                 Width="200"
                 Text="{Binding SelectedNote.Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="1" Text="Content:" FontWeight="Bold"/>
        <TextBox Grid.Row="1" Margin="50,0,0,0"
                 Width="300" Height="150"
                 TextWrapping="Wrap" AcceptsReturn="True"
                 Text="{Binding SelectedNote.Content, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
    </Grid>
</UserControl>
